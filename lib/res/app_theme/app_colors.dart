import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color lightTextcolor = Color(0xFFFFFFFF);
  static const Color lightPrimary = Color(0xFFF59EC1); // Blue
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightText = Color(0xFF000000);
  static const Color lightclipRRback = Color(0xFFF2F4FF);

  // Dark Theme Colors
  static const Color darkTextcolor = Color(0xFFFFFFFF);
  static const Color darkPrimary = Color(0xFF0D47A1); // Darker Blue
  static const Color darkBackground = Colors.black;
  static const Color darkText = Color(0xFFFFFFFF);
  static const Color darkclipRRback = Color.fromARGB(255, 137, 137, 137);

  // Common Colors
  static const Color accent = Color.fromARGB(255, 195, 195, 194); // Amber
  static const Color error = Colors.redAccent;
  static const Color done = Colors.green;

  static const Color grey = Colors.grey;
}
